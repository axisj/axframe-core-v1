import * as React from "react";
import styled from "@emotion/styled";

interface Props {}

function StatChart01({}: Props) {
  return <Container>StatChart01</Container>;
}

const Container = styled.div``;

export { StatChart01 };
